# Repositories

Folder này dành cho repository implementation dùng Prisma hoặc nguồn dữ liệu khác.

Prompt 03 chưa tách repository thật. Khi tách, cần ưu tiên tenant scope, transaction boundary và các query đang dùng `$queryRawUnsafe`.
