# DTOs

Folder này dành cho request/response DTO giữa presentation layer và application layer.

Không đổi public API contract trong Prompt 03. DTO thật sẽ được thêm khi tách controller khỏi `backend/src/api/dashboard.js`.
